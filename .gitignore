node_modules
dist
.env
uploads/*
!uploads/.gitkeep
.vercel
.env*.local

# Claude Code: personal overrides only; .claude/settings.json is shared
.claude/settings.local.json
.claude/worktrees/

# npm run seed:dhaka writes a backup of the salons it replaces here
seed-backups/
